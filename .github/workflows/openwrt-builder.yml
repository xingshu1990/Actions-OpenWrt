name: OpenWrt Builder Enhanced

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      test_mode:
        description: '测试模式（只下载不编译）'
        required: false
        default: 'false'

env:
  REPO_URL: https://github.com/coolsnowwolf/lede
  REPO_BRANCH: master
  FEEDS_CONF: feeds.conf.default
  CONFIG_FILE: .config
  DIY_P1_SH: diy-part1.sh
  DIY_P2_SH: diy-part2.sh
  
  CCACHE_DIR: ${{ github.workspace }}/ccache
  CCACHE_MAXSIZE: 5G
  
  UPLOAD_BIN_DIR: false
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true
  UPLOAD_PACKAGES: true   # ✅ 新增
  
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04
    if: github.event.repository.owner.id == github.event.sender.id || github.event_name == 'workflow_dispatch'

    steps:
    - name: 检出代码
      uses: actions/checkout@main

    - name: Initialization environment
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
        sudo docker image prune --all --force
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool lrzsz mkisofs msmtp ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 python3-pyelftools python3-setuptools qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
        sudo timedatectl set-timezone "$TZ"
        sudo mkdir -p /workdir
        sudo chown $USER:$GROUPS /workdir

    - name: 创建缓存目录
      run: mkdir -p ${{ env.CCACHE_DIR }}

    - name: 恢复 ccache 缓存
      uses: actions/cache@v4
      with:
        path: ${{ env.CCACHE_DIR }}
        key: ccache-${{ runner.os }}-${{ github.ref }}-${{ github.sha }}
        restore-keys: |
          ccache-${{ runner.os }}-${{ github.ref }}-
          ccache-${{ runner.os }}-

    - name: 克隆源码
      run: git clone --depth=1 $REPO_URL -b $REPO_BRANCH openwrt

    - name: 加载 feeds
      run: |
        [ -e $FEEDS_CONF ] && cp $FEEDS_CONF openwrt/feeds.conf.default
        if [ -f $DIY_P1_SH ]; then
          chmod +x $DIY_P1_SH
          cd openwrt
          $GITHUB_WORKSPACE/$DIY_P1_SH
        fi

    - name: 更新 feeds
      run: cd openwrt && ./scripts/feeds update -a

    - name: 安装 feeds
      run: cd openwrt && ./scripts/feeds install -a

    - name: 加载配置
      run: |
        [ -e files ] && cp -r files openwrt/
        [ -e $CONFIG_FILE ] && cp $CONFIG_FILE openwrt/.config
        if [ -f $DIY_P2_SH ]; then
          chmod +x $DIY_P2_SH
          cd openwrt
          $GITHUB_WORKSPACE/$DIY_P2_SH
        fi

    - name: 下载软件包
      run: |
        cd openwrt
        make defconfig
        make download -j8
        find dl -size -1024c -delete

    - name: 编译固件
      id: compile
      if: github.event.inputs.test_mode != 'true'
      run: |
        cd openwrt
        export PATH="/usr/lib/ccache:$PATH"
        export CCACHE_DIR=${{ env.CCACHE_DIR }}
        
        if make -j$(nproc); then
          echo "编译成功"
        else
          make -j1 V=s
        fi
        
        echo "status=success" >> $GITHUB_OUTPUT
        
        grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' > DEVICE_NAME
        [ -s DEVICE_NAME ] && echo "DEVICE_NAME=_$(cat DEVICE_NAME)" >> $GITHUB_ENV
        
        echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV
        echo "FILE_TIME=$(date "+%m月%d日-%H点%M分")" >> $GITHUB_ENV
        echo "RELEASE_TAG=$(date +"%Y.%m.%d-%H%M")" >> $GITHUB_ENV

    # ==================== ✅ 新增：收集IPK ====================
    - name: 收集IPK包
      id: collect_packages
      if: env.UPLOAD_PACKAGES == 'true' && steps.compile.outputs.status == 'success'
      run: |
        cd openwrt
        mkdir -p ${{ github.workspace }}/all_packages
        
        if [ -d bin ]; then
          find bin -name "*.ipk" -type f | while read ipk; do
            if [[ ! "$ipk" =~ "kernel" ]]; then
              cp "$ipk" ${{ github.workspace }}/all_packages/
            fi
          done
        fi
        
        if [ -d "${{ github.workspace }}/all_packages" ]; then
          PKG_COUNT=$(ls -1 ${{ github.workspace }}/all_packages/*.ipk 2>/dev/null | wc -l)
          echo "package_count=$PKG_COUNT" >> $GITHUB_OUTPUT
          echo "packages_dir=${{ github.workspace }}/all_packages" >> $GITHUB_ENV
        else
          echo "package_count=0" >> $GITHUB_OUTPUT
        fi

    # ==================== ✅ 新增：上传IPK ====================
    - name: 上传IPK包
      uses: actions/upload-artifact@main
      if: steps.collect_packages.outputs.package_count != '0'
      with:
        name: OpenWrt_packages${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
        path: ${{ env.packages_dir }}

    - name: 上传 bin 目录
      uses: actions/upload-artifact@main
      if: steps.compile.outputs.status == 'success' && env.UPLOAD_BIN_DIR == 'true'
      with:
        name: OpenWrt_bin${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
        path: openwrt/bin

    - name: 整理固件
      id: organize
      if: env.UPLOAD_FIRMWARE == 'true' && steps.compile.outputs.status == 'success'
      run: |
        cd openwrt/bin/targets/*/*
        rm -rf packages
        echo "FIRMWARE=$PWD" >> $GITHUB_ENV
        echo "status=success" >> $GITHUB_OUTPUT

    - name: 上传固件
      uses: actions/upload-artifact@main
      if: steps.organize.outputs.status == 'success'
      with:
        name: OpenWrt_firmware${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
        path: ${{ env.FIRMWARE }}

    - name: 生成 Release
      id: tag
      if: env.UPLOAD_RELEASE == 'true' && steps.compile.outputs.status == 'success'
      run: |
        echo "release_tag=${{ env.RELEASE_TAG }}" >> $GITHUB_OUTPUT
        
        cat > release.txt << EOF
        固件编译完成
        
        编译时间: ${{ env.FILE_TIME }}
        源码: ${{ env.REPO_URL }}
        分支: ${{ env.REPO_BRANCH }}
        EOF
        
        echo "status=success" >> $GITHUB_OUTPUT

    - name: 发布 Release
      uses: softprops/action-gh-release@master
      if: steps.tag.outputs.status == 'success'
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ steps.tag.outputs.release_tag }}
        name: "OpenWrt 固件 ${{ env.FILE_TIME }}"
        body_path: release.txt
        files: ${{ env.FIRMWARE }}/*
